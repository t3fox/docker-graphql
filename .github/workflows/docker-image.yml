name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Docker login
      env:
        DOCKER_USER: ${{ SECRETS.DOCKER_USER }}
        DOCKER_PASSWORD: ${{ SECRETS.DOCKER_PASSWORD }}
      run: |
        echo "Iniciando login"
        docker login -u $DOCKER_USER -p $DOCKER_PASSWORD
        echo "Fin del login"

    - name: Build Docker Image
      run: |
        docker build -t t3fox/docker-graphql:0.0.1 .
        
    - name: Push Docker Image
      run: |
        docker push t3fox/docker-graphql:0.0.2 
    
    
    
#    - name: Build the Docker image
#      run: docker build . --file Dockerfile --tag t3fox/docker-graphql:$(date +%s)
